# v1(규칙 기반 + stdlib http.server)은 외부 의존성이 없다 — 이 파일은 비어 있어도 됨.
#
# 운영에서 실제 PostgreSQL에 붙일 때만 아래 주석을 해제한다.
# 전부 jfrog PyPI 리모트에서 설치된다 (Dockerfile의 JFROG_PYPI 참고).
#
# psycopg[binary]>=3.1     # PostgreSQL read-only 커넥션
#
# FastAPI로 승격해 인증/권한/미들웨어를 얹을 경우:
# fastapi>=0.110
# uvicorn>=0.29
